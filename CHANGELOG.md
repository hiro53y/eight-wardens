# CHANGELOG

## 2026-05-17 UI/PWAスマホ横向き修正

### ホーム画面
- 飾り扱いだった「お知らせ」「特典受取」「ローカルセーブ待機」を撤去。
- タイトルロゴ、隊員列、主要4ボタンが重ならないように再配置。
- Android横向きの低い表示高でも下部ボタンが隠れにくい余白へ調整。

### マップ画面
- 上部ナビゲーションを低背化。
- ロック済みノードは大量のLOCK文字ではなく、暗色化と鍵形状で表現。
- ステージ詳細パネルは内部スクロールに変更し、出撃ボタンを押せる位置に維持。

### ユニット管理画面
- 3カラムの高さ配分をスマホ横向き向けに再設計。
- 左ユニット一覧とクラスタリーを内部スクロール化。
- ユニット名を2行まで表示し、極端な省略を軽減。
- ポートレートシートの表示比率を見直し、縦伸び感を抑制。
- 右側の編成プレビューと操作ボタンが下端で切れにくいサイズへ調整。

### 図鑑画面
- 敵一覧、職業一覧をカード型UIとして再調整。
- 各一覧領域だけがスクロールする構造に変更。
- 黒基調、金縁、カード背景の質感を統一。

### PWA対応
- Service Worker登録を `/service-worker.js` に統一。
- 互換用に `/sw.js` も残し、両方をアプリシェルキャッシュ対象に追加。
- Cloudflare Pages向け `_headers` にService Workerのno-cache設定を追加。

### レスポンシブ対応
- 通常ブラウザ表示ではAndroid Chrome下部UIを避ける保守的な下余白を追加。
- standalone起動時はsafe-area中心の余白へ切り替え。
- `dvh`、`clamp()`、内部スクロールを使い、全体スクロールで画面が押し出される状態を抑制。

### 既知の制約
- 個別キャラクター画像ではなく、1枚のポートレートシートをCSSで切り出している。
- 装備、実績詳細、討伐記録、音声、ドラッグ編成は未実装。
- この実行環境ではVite/esbuild起動が `spawn EPERM` で止まり、`npm run build` の最終工程は未完了。
